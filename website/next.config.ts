import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  distDir: 'dist', // Specify your custom build directory
  // output: 'export',
  // images: {
  //   unoptimized: true,  // Disable image optimization in static export mode
  // },
};

export default nextConfig;
